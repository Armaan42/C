#include<stdio.h>

void main(){
    char arr[40];
    printf("enter the passphrase : ");
    scanf("%s",&arr);

    if(arr != ipaddr){
        printf("Better Luck next time");

    }
    else{
        char fake[40];
        for(int i=1; ;i++){
            printf("http://%d.%d.%d.%d:%d",);
        }
    }
}