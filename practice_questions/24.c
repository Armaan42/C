//Write a program to output one-digit password without showing it at runtime.

#include<stdio.h>
#include<conio.h>

int main(){
	char password;
	printf("Enter the password : ");
	password = getch();
	printf("*");
	return 0;
}