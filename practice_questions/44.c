// Write a program to copy the elements from one array into another.

#include<stdio.h>

int main(){
	int arr[5];
	int brr[5];
	for(int i=0;i<a;i++){
		printf("enter the %d array : ",i);
		scanf("%d",&arr[i]);
	}

	for(int i=0;i<a;i++){
		brr[i] = arr[i];
	}

	for(int i=0;i<a;i++){
		printf("%d",brr[i]);
	}
	return 0;
}